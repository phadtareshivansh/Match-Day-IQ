// Only edit the line below.
//
// If frontend and backend are served by the SAME app (recommended — see
// README "Deploying it for real matchday use"), leave this as "/api" and
// stop reading.
//
// If your frontend is hosted separately (e.g. on Vercel) from your backend
// (e.g. on Railway), replace "/api" with your backend's full URL + "/api",
// for example: "https://match-day-iq-production.up.railway.app/api"
window.MATCHDAY_API_BASE = "/api";
